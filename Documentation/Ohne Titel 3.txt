Test

1. drop both schemas EKC_... and OKC_...
2. drop the t2 ables DATACHANGELOG from database kanu
3. mvn liquibase:clearCheckSums
4. rerun kcserver
5. login with cschog for group/Schema EKC_...
	=> schema EKC_... is created with the needed tables and colummns
6. CRUD for table verein is working, althoug there were still some data which I have entered before, but I habve dropped the schema...
7. login with user dschaefer and schema OKC_...
	=> schema OKC_... is created with the needed tables and colummns
8. switching to button VEREIN
	=> there are still data available (I don´t know from where, because I have dropped the schema)
	=> now the app is hanging, the message "Failed to fetch the active schema." appears