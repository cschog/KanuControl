spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:postgresql://localhost:5432/kanu?currentSchema=public
    username: kcuser
    password: ""
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        multiTenancy: NONE

  liquibase:
    enabled: true

keycloak:
  enabled: false